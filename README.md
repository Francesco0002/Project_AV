## Setup
```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt

Config path dataset

Crea configs/local_paths.json:

{ "soccernet_root": "C:\\path\\to\\soccernet" }
